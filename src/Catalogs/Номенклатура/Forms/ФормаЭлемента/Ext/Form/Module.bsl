
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(Объект.Наименование) Тогда
		ДанныеОтбора = Новый Структура("Номенклатура", Объект.Ссылка);
		ДанныеРегистра = РегистрыСведений.ЦеныНоменклатуры.СрезПоследних(ТекущаяДата(), ДанныеОтбора);
		Для Каждого Строка Из ДанныеРегистра Цикл
			ТабСтрока = Объект.Цены.Добавить();
			ЗаполнитьЗначенияСвойств(ТабСтрока, Строка);
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры
