
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	ФИО = Объект.Фамилия + " " + Объект.Имя;
	Если ЗначениеЗаполнено(Объект.Отчество) Тогда
		ФИО = ФИО + " " + Объект.Отчество;
	КонецЕсли;
	Объект.Наименование = ФИО;
КонецПроцедуры
