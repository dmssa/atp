Процедура ПриЗаписи(Отказ)
	ОдинСклад = Константы.ИспользоватьОдинСклад.Получить();
	Если ОдинСклад = ЭтотОбъект.Значение Тогда
		Константы.ИспользоватьОдинСклад.Установить(НЕ ЭтотОбъект.Значение);
	КонецЕсли;
КонецПроцедуры
