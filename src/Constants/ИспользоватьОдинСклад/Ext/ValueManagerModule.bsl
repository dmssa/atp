Процедура ПриЗаписи(Отказ)
	НесколькоСкладов = Константы.ИспользоватьНесколькоСкладов.Получить();
	Если НесколькоСкладов = ЭтотОбъект.Значение Тогда
		Константы.ИспользоватьНесколькоСкладов.Установить(НЕ ЭтотОбъект.Значение);
	КонецЕсли;
КонецПроцедуры